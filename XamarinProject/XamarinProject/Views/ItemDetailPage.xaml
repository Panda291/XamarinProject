<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="XamarinProject.Views.ItemDetailPage"
             Title="{Binding Title}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Remove" Clicked="Remove_Clicked" />
    </ContentPage.ToolbarItems>
    
    <StackLayout Spacing="20" Padding="15">
        <Label Text="Name:" FontSize="Medium" />
        <Label Text="{Binding Card.Name}" d:Text="Name" FontSize="Small"/>
        <Label Text="Type:" FontSize="Medium" />
        <Label Text="{Binding Card.Type}" d:Text="Type" FontSize="Small"/>
        <Label Text="Attribute:" FontSize="Medium" />
        <Label Text="{Binding Card.Attribute}" d:Text="Type" FontSize="Small"/>
    </StackLayout>
    
</ContentPage>